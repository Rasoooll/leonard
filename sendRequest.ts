import { Positions } from './profile';
import {bybit} from "./init";

// set close by for open posiosion
export async function setCB(){
    // await bybit.createOrder("ETH/USDT" , 'limit' , "sell" , 0.01 , 1230.22 , params:{} ) , 
    await bybit.
}